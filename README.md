# fun-with-trae

#first project
# Angular + Node.js + Socket.IO + PostgreSQL chat app

#Angular Frontend:
#Handles UI, message input, and real-time message display via Socket.IO.

#(Node.js + Express) Backend:
#Manages authentication, database (PostgreSQL), and Socket.IO server.
#npm install express socket.io pg cors dotenv

#(PostgreSQL) Database:
#Stores users, messages, chat rooms.


#installed supabase
#npm install @supabase/supabase-js

